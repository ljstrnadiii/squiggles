{
  "name": "@squiggles/aws",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "preview": "pnpm build && pulumi preview",
    "up": "pnpm build && pulumi up"
  },
  "dependencies": {
    "@pulumi/aws": "7.43.0",
    "@pulumi/pulumi": "3.259.0"
  },
  "devDependencies": {
    "@types/node": "26.2.0",
    "typescript": "7.0.2"
  }
}
